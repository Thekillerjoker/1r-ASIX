# 1. Comprovar proces nano actiu:
## Crea un programa en Python que cada 5 segundos ejecute un comando del sistema para contar cuántos procesos con el nombre nano están activos,y mostrar por pantalla la salida del comando de esta manera: Procesos nano (numero de procesos). El programa debe terminar automáticamente cuando el número de procesos sea 0.
# 2. Comprovar que un proces o progrma introduit está actiu.
## Haz un programa donde una variable contenga el nombre de un proceso (por ejemplo "python").Cada 3 segundos debe comprobar si ese proceso sigue activo. Cuando deje de existir, el programa debe finalizar sin usar.
# 3. Esperar a que un proceso empieze:
## Escribe un programa que esté en un bucle infinito comprobando si un proceso llamado "input" aparece en la lista de procesos.Cuando detecte que se ha iniciado, debe mostrar un mensaje y terminar el programa.
# 4. Contador de procesos:
## Crea un programa que cada 10 segundos muestre cuántos procesos "ssh" hay en ejecución.El programa debe ejecutarse durante exactamente 1 minuto y luego terminar
